<Window x:Class="OutilGestionAbsences.View.StudentAbsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OutilGestionAbsences.View"
        mc:Ignorable="d"
        Title="StudentAbsView" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Label Content="Étudiant :" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding StudentFullName}" VerticalAlignment="Center" FontWeight="Bold" Margin="5,0"/>
        </StackPanel>
        <ListView Grid.Row="1" Margin="10" ItemsSource="{Binding Absences}"/>
    </Grid>


</Window>
