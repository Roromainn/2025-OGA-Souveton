<Window x:Class="OutilGestionAbsences.View.StudentAbsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OutilGestionAbsences.View"
        mc:Ignorable="d"
        Title="Vue des absences de l'étudiant" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Label Content="Étudiant :" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBlock Text="{Binding StudentFullName}" VerticalAlignment="Center" FontWeight="Bold" Margin="5,0" FontSize="14"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Margin="10" ItemsSource="{Binding Absences}" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding FormattedDate}" Width="100"/>
                <DataGridTextColumn Header="Heure" Binding="{Binding FormattedTime}" Width="80"/>
                <DataGridTextColumn Header="Cours" Binding="{Binding CourseName}" Width="150"/>
                <DataGridTextColumn Header="Enseignant" Binding="{Binding TeacherName}" Width="120"/>
                <DataGridTextColumn Header="Durée (min)" Binding="{Binding Duration}" Width="80"/>
                <DataGridTextColumn Header="Justifiée" Binding="{Binding JustifiedStatus}" Width="80"/>
                <DataGridTextColumn Header="Justification" Binding="{Binding Justification}" Width="250"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="0,1,0,0" Margin="0,10,0,0" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Durée totale absences :" FontWeight="Bold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding TotalDuration}" Margin="0,0,30,0" VerticalAlignment="Center"/>
                <TextBlock Text="Durée non justifiée :" FontWeight="Bold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding TotalDurationUnjustified}" VerticalAlignment="Center"/>
                <TextBlock Text=" min" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Button Grid.Row="3" Content="Fermer" Width="100" Height="40" Margin="10" HorizontalAlignment="Right" Click="CloseButton_Click"/>
    </Grid>

</Window>
